# /etc/hcibridge.conf - options for the hcibridge daemon.
# Sourced by the runit and s6 run scripts.
#
# Leave BRIDGE_ARGS empty for auto-discovery (finds every bridge on the LAN,
# each gets its own adapter). To pin one board instead:
#   BRIDGE_ARGS="--host 172.16.135.242"
BRIDGE_ARGS=""
